# Production env for docker-compose.prod.yml (copy to .env on VPS)
VITE_SUPABASE_URL=https://api.fasted.360web.cloud
VITE_SUPABASE_ANON_KEY=your-anon-key-here
APP_PUBLISH_PORT=8022

# PocketBase fallback (commented out after Supabase cutover — kept for rollback)
# VITE_POCKETBASE_URL=https://api.fasted.360web.cloud
# POCKETBASE_PUBLISH_PORT=8023
