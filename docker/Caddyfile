{
	# Replace with your email for Let's Encrypt notifications.
	email you@example.com
}

# API — PocketBase (update domain before deploying)
api.example.com {
	reverse_proxy pocketbase:8090
}

# App — static PWA build (optional; mount dist/ to /srv in compose if used)
# app.example.com {
# 	root * /srv
# 	encode gzip
# 	try_files {path} /index.html
# 	file_server
# }

# Local dev: uncomment and run `docker compose up` then use http://localhost
# :8090 {
# 	reverse_proxy pocketbase:8090
# }
